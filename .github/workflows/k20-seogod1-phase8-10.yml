name: K20 SEO God1 Phase08-10 Live Execution
on:
  push:
    branches: [main]
    paths:
      - 'seo-god1-ops/phase08-10-*.json'
      - 'scripts/k20_seogod1_phase8_10.py'
      - '.github/workflows/k20-seogod1-phase8-10.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: k20-seogod1-phase8-10
  cancel-in-progress: false
jobs:
  execute:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
      WP_USERNAME: ${{ secrets.WP_USERNAME }}
      WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 2}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - run: python scripts/k20_seogod1_phase8_10.py
      - shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f seo-god1-results/phase08-10-live-execution.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'seo-god1: record phase08-10 live execution'
          for attempt in 1 2 3 4; do
            git pull --rebase origin main
            if git push origin main; then exit 0; fi
            sleep $((attempt*2))
          done
          exit 1
