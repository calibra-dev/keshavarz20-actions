name: K20 GrowthOS Phase 9 Agent Flow
on:
  push:
    branches: [main]
    paths:
      - 'growthos-phase9-ops/*.json'
      - 'growthos-phase9-assets/*.html'
      - 'scripts/k20_growthos_phase9_agent_flow.py'
      - '.github/workflows/k20-growthos-phase9-agent-flow.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: k20-growthos-phase9-agent-flow
  cancel-in-progress: false
jobs:
  execute:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
      WP_USERNAME: ${{ secrets.WP_USERNAME }}
      WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 2}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - run: python -m py_compile scripts/k20_growthos_phase9_agent_flow.py
      - run: python scripts/k20_growthos_phase9_agent_flow.py
      - shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f growthos-phase9-results/flow-qa.json growthos-phase9-results/summary.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "growthos phase9: record verified SXO agent flow"
          for attempt in 1 2 3 4 5; do
            git pull --rebase origin main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
