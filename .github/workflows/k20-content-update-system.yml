name: K20 Content Update System

on:
  push:
    branches: [main]
    paths:
      - 'phase2-content-update-ops/*.json'
  workflow_dispatch:
  schedule:
    - cron: '40 3 * * 1'

permissions:
  contents: write

concurrency:
  group: k20-content-update-system
  cancel-in-progress: false

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
      WP_USERNAME: ${{ secrets.WP_USERNAME }}
      WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 2 }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: python -m pip install requests
      - run: python scripts/k20_content_update_system.py phase2-results/content-update-queue.json
      - shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add phase2-results/content-update-queue.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "phase2: refresh content update queue"
          git pull --rebase origin main
          git push origin main
