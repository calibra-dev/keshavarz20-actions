name: K20 Phase16 Deep Diagnostic
on:
  push:
    branches: [main]
    paths:
      - 'scripts/k20_phase16_deep_diagnostic.py'
      - '.github/workflows/k20-phase16-deep-diagnostic.yml'
permissions:
  contents: read
jobs:
  diagnose:
    runs-on: ubuntu-latest
    env:
      WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
      WP_USERNAME: ${{ secrets.WP_USERNAME }}
      WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - run: python scripts/k20_phase16_deep_diagnostic.py
