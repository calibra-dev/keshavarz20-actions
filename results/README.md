# Gateway results

The guarded cloud gateway writes only compact, non-secret result summaries here. Full credentials are never written. Price and discount fields are intentionally excluded from product read summaries.

Because this repository is public, results must remain limited to operational IDs/statuses and public-safe metadata. Private draft bodies and customer/admin data must use the private WPVibe/Abilities route instead.
